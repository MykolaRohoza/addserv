 sudo apt-get install php7.1-mbstring
 sudo apt-get install php7.1-zip
 sudo apt-get install php7.1-xmlrpc php7.1-xml
 sudo apt-get install php7.1-gd
 sudo apt-get install php7.1-intl
 sudo apt-get install php7.1-xsl
 
 
 
 
   db:
    image: mysql:latest
    container_name: f_mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: example
    expose: 
      - 3388
	  
	  
	  
	  
mysql -uroot -pgjGhs5DrABcy; CREATE DATABASE my_parcel; GRANT ALL PRIVILEGES ON my_parcel.* TO root@localhost IDENTIFIED BY 'gjGhs5DrABcy';



mysql -uroot -proot \g mysql CREATE DATABASE my_parcel \g mysql GRANT ALL PRIVILEGES ON my_parcel.* TO root@localhost IDENTIFIED BY 'root' \g mysql exit

sudo cp /home/rohoza/Desktop/addserv /usr/bin/addserv


sudo cp /var/www/addserv/addserv /usr/bin/addserv

sudo cp /usr/bin/addserv /var/www/addserv/addserv
